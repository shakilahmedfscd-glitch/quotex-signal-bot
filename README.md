# Quotex Mobile Signal Bot V2
Uploads: index.html, style.css, app.js, manifest.json, sw.js.

V2 adds support/resistance, countdown, performance metrics and CSV historical backtesting.

CSV format: time,open,high,low,close

The live screen still uses demo candles. No Quotex orders are placed. Confidence is an internal score, not a guaranteed probability.